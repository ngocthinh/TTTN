<%= form_tag action: "create" do %>  
  <%= hidden_field_tag :product_id, @products.id %>  
  <input id="count" name="count" type="number" min="1" value = 1 class="form-control" />
  <%= submit_tag "add to cart", class: "btn btn-primary"  %> 
<% end %>  